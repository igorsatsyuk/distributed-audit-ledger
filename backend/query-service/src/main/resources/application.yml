server:
  port: 8084

spring:
  application:
    name: query-service

  r2dbc:
    url: ${R2DBC_URL:r2dbc:postgresql://localhost:5432/auditdb}
    username: ${DB_USERNAME:audit_user}
    password: ${DB_PASSWORD:audit_pass}

  flyway:
    enabled: false   # Query service does NOT run migrations — event-store owns the schema

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics

logging:
  level:
    lt.satsyuk.distributed.audit: DEBUG
