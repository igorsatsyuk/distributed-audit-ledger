{
  "name": "distributed-audit-ledger-blockchain",
  "version": "0.1.0",
  "private": true,
  "description": "Smart contract workspace for Distributed Audit Ledger",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "coverage:js": "c8 --reports-dir coverage-js --reporter lcov --reporter text mocha \"test-js/**/*.test.js\"",
    "deploy:localhost": "hardhat run scripts/deploy.js --network localhost",
    "deploy:ganache": "hardhat run scripts/deploy.js --network ganache"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "c8": "^10.1.3",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.12",
    "solidity-coverage": "^0.8.17"
  }
}

